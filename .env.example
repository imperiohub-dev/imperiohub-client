# API Configuration
# Development Local: http://localhost:3000
# Development Remote: https://api.imperiohub.com
# Production: https://api.imperiohub.com
#
# IMPORTANTE:
# - NO incluir /api/v1 al final, el sistema lo agrega automáticamente
# - Si VITE_API_URL empieza con 'https', el proxy de Vite se activa automáticamente
#   y redirige /api -> VITE_API_URL/api (con cookies configuradas para localhost)
# - Si VITE_API_URL es 'http://localhost', NO se usa proxy (desarrollo local directo)
# - En producción, siempre se usa la URL completa sin proxy
VITE_API_URL=http://localhost:3000

# Application
VITE_APP_NAME=ImperioHub Trading
VITE_APP_VERSION=1.0.0
